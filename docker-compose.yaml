version: '3.8'

services:
  shelflife-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_BASE_URL: http://localhost:8080
    ports:
      - "5173:5173"
    restart: unless-stopped